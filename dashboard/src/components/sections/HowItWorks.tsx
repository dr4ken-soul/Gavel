import { FadeIn } from '../ui/FadeIn'

const layers = [['01', 'Commit', 'Buyer locks the payment with keccak of the spec and keccak of the rubric plus salt. The standard exists before the work does.'], ['02', 'Deliver', 'Seller agent posts the artifact hash before the deadline. The contract records the confirmed delivery.'], ['03', 'Rule', 'Judge reveals rubric and salt, contract verifies the commitment and signature, verdict settles.'], ['04', 'Settle', 'Approve pays the seller, reject refunds the buyer, receipt emitted either way.']]

/** Explains the four confirmed movements from escrow to receipt. */
export function HowItWorks(): JSX.Element { return <section id="how" className="border-t border-[var(--border-subtle)] bg-[var(--bg-secondary)] py-24 md:py-32"><div className="mx-auto max-w-4xl px-4 md:px-8"><FadeIn><h2 className="text-center font-display text-3xl tracking-tight text-[var(--text-primary)] md:text-5xl">Four movements, one receipt.</h2></FadeIn><div className="mt-16 flex flex-col gap-4">{layers.map(([number, title, copy], index) => <FadeIn key={number} delay={index * 0.15}><div className="flex items-start gap-4 rounded-[8px] border border-[var(--border-default)] p-6 transition-colors duration-200 hover:border-white/20"><span className="min-w-[2.5rem] font-mono text-sm text-[var(--text-muted)]">{number}</span><div><h3 className="font-body text-base font-medium text-[var(--text-primary)]">{title}</h3><p className="mt-1 font-body text-sm leading-relaxed text-[var(--text-secondary)]">{copy}</p></div></div></FadeIn>)}</div></div></section> }

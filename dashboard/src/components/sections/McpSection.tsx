import { Check, Copy, FileCheck, ShieldCheck, Users } from 'lucide-react'
import { useState } from 'react'
import { FadeIn } from '../ui/FadeIn'

/** Explains the four-tool MCP integration and provides a copyable command. */
export function McpSection(): JSX.Element { const [copied, setCopied] = useState(false); const command = 'npx gavel-mcp'; const copy = async () => { await navigator.clipboard.writeText(command); setCopied(true); window.setTimeout(() => setCopied(false), 1500) }; return <section className="border-t border-[var(--border-subtle)] bg-[var(--bg-primary)] py-24 md:py-32"><div className="mx-auto grid max-w-[1400px] items-center gap-12 px-4 md:grid-cols-2 md:px-8"><FadeIn delay={0.3}><h2 className="font-display text-3xl tracking-tight text-[var(--text-primary)] md:text-5xl">Any agent can hire. Any agent can work.</h2><p className="mt-4 max-w-[45ch] font-body text-base leading-relaxed text-[var(--text-secondary)]">Gavel ships as an MCP server. Four tools, one rail, no custom integration. If your framework speaks MCP it can open an escrow in one call.</p><div className="mt-7 space-y-5">{[[ShieldCheck, 'The rubric commitment travels with the job.'], [FileCheck, 'Delivery is a content hash, not a private claim.'], [Users, 'The judge is independent from buyer and seller.']].map(([Icon, text]) => <div key={text as string} className="flex items-start gap-3"><Icon size={20} className="mt-0.5 text-[var(--accent)]" /><p className="text-sm text-[var(--text-secondary)]">{text as string}</p></div>)}</div></FadeIn><FadeIn delay={0.5}><div className="overflow-hidden rounded-[8px] border border-[var(--border-default)] bg-[var(--bg-elevated)]"><div className="flex h-9 items-center gap-2 border-b border-[var(--border-subtle)] bg-[var(--bg-surface)] px-4"><span className="h-2.5 w-2.5 rounded-full bg-[var(--text-muted)]" /><span className="ml-2 font-mono text-xs text-[var(--text-muted)]">gavel · mcp</span></div><div className="p-6 font-mono text-sm leading-7 text-[var(--text-secondary)]"><div className="text-[var(--text-muted)]">$ {command}</div><div className="mt-4">tool call create_job</div><div>spec: five landing headlines for Somno</div><div>rubric: five binary checks, hash committed</div><div>escrow: 0.05 OKB</div><div className="t-approve">→ confirmed job receipt returned</div><button onClick={copy} className="mt-5 flex min-h-11 items-center gap-2 border border-[var(--border-default)] px-3 text-xs text-[var(--text-secondary)] hover:text-[var(--text-primary)]">{copied ? <Check size={16} /> : <Copy size={16} />} {copied ? 'copied' : 'Copy command'}</button></div></div></FadeIn></div></section> }

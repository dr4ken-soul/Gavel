/** Adds the low contrast technical grain layer used across the landing page. */
export function GrainOverlay(): JSX.Element { return <div aria-hidden="true" className="pointer-events-none fixed inset-0 z-[60] opacity-[0.08] grain-overlay" /> }
